# Runtime dependencies — what the serving path (api/ + src/generation + src/retrieval) imports.
# Ingestion + evaluation deps live in requirements-dev.txt.
fastapi
uvicorn[standard]
openai
chromadb
rank-bm25
cohere
python-dotenv
requests
PyJWT
cryptography
