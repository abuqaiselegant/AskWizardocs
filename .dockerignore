venv/
__pycache__/
*.pyc
*.pyo
.env
.DS_Store
**/.DS_Store
# Data: bake in only the runtime-needed artifacts (chroma_db/ + chunks.jsonl).
# Exclude ingestion-only data — 666 MB of raw HTML and the intermediate docs.jsonl.
rag-dataset/data/raw/
rag-dataset/data/processed/docs.jsonl
rough.ipynb
# README assets — never COPYied into the image, so keep them out of the build context.
docs/
# Frontend ships to Vercel, not into this image — keeps node_modules/dist out of the build context.
frontend/
.git/
.claude/
CLAUDE.md
PLAN.md
DEPLOY.md
tests/
scripts/
src/evaluation/eval_results.jsonl
