# Dev-only dependencies — NOT installed in the production image.
# Install locally with: pip install -r requirements-dev.txt
-r requirements.txt

# Ingestion pipelines (scripts/)
trafilatura
beautifulsoup4
lxml
tiktoken

# Evaluation (src/evaluation/) — pulls the heavy datasets/pandas/pyarrow stack
ragas
langchain-openai

# Tests (tests/) — pure-function suite, needs no keys and no corpus.
# Run from the repo root so `src.` resolves: pytest tests/ -q
pytest

# Future: file uploads for bring-your-own-docs (not used yet)
python-multipart
